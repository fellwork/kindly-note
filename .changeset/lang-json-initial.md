---
'@kindly-note/lang-json': minor
---

Initial release of `@kindly-note/lang-json`: the JSON / JSONC / JSON5 language definition. The default export is a deep-frozen `LanguageDefinition` (spec §0 architectural shift #1 — languages-as-values). Aliases: `json`, `jsonc`, `json5`. First real `@kindly-note/lang-*` package — exercises the cohort-3a deepened matcher end-to-end.
